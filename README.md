Before creating a virtualenv install picamera2 and python3-libcamera

Then create virtualenv with an arg

```python -m venv --system-site-packages venv```

Then activate the virtualenv and install poetry using

```pip install poetry```

Then run 

```poetry install```

